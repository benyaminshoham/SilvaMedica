# Silva Medica Pitch Deck

A beautifully designed, interactive pitch deck for Silva Medica - a herbal medicine simulation game.

## 🚀 Getting Started

### Prerequisites
- Node.js 18+ installed on your machine

### Installation

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm run dev
```

3. Open your browser to the URL shown (usually `http://localhost:5173`)

### Build for Production

To create a production-ready build:
```bash
npm run build
```

The built files will be in the `dist` folder, ready to deploy.

## 📄 Export as PDF

1. Open the application in your browser
2. Press `Ctrl + P` (Windows) or `Cmd + P` (Mac)
3. Select "Save as PDF" as destination
4. Make sure "Background graphics" is enabled
5. Save your PDF

## 🌐 Deploy

You can deploy this to:
- **Vercel**: `npm i -g vercel && vercel`
- **Netlify**: Drag the `dist` folder to netlify.com/drop
- **GitHub Pages**: Push to GitHub and enable Pages in settings

## 🎨 Customization

Edit `/App.tsx` to modify slide content, colors, or layout.

## 📦 Tech Stack

- React 18
- TypeScript
- Tailwind CSS v4
- Vite
- Lucide React (icons)
